import { describe, it, expect } from 'vitest';

describe('E2E: User Onboarding Flow', () => {
  it('should define visitor to registered founder flow', () => {
    const landingRoute = '/';
    expect(landingRoute).toBe('/');
  });
});
